#include "../backbone.h"
inherit STD_WEAPON;

void setup(void){
    
    
   set_name("saw");
   set_alias("meat saw");
   set_short("a meat saw");
   set_long("A nuclear-driven meat saw. Although in the meat world, this is the saw-of-saws, "+
      "as a weapon, it's not so good.\n");
   set_class(10);
   set_weight(2);
   set_value(550);
}
