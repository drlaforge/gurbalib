#include "../backbone.h"
inherit STD_WEAPON;
void setup(void){
    
    
   set_name("chain");
   set_alias("weapon");
   set_short("a chain");
   set_long("A thick chain which could be used as a weapon.\n");
   set_wc(5);
   set_value(8);
   set_weight(1);
}
