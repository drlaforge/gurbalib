#include "../backbone.h"
inherit STD_WEAPON;
void setup(void){
    
    
   set_name("rambo knife");
   set_alias(({"knife","weapon"}));
   set_short("a rambo knife");
   set_long("The knife looks quite old and dull, never-the-less it could still cause pain.\n");
   set_wc(9);
   set_value(25);
   set_weight(2);
}
