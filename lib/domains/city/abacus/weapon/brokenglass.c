#include "../backbone.h"
inherit STD_WEAPON;
void setup(void){
    
    
   set_name("broken glass");
   set_alias(({"sharp broken glass","sharp glass","glass"}));
   set_short("a sharp broken glass");
   set_long_f("A sharp broken glass, often used by drunks in a fight.\n");
   set_class(5);
   set_weight(2);
   set_value(50);
}
